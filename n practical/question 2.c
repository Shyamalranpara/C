#include<stdio.h>
int sum(){
    int n,sum=0,size;
    
    printf("enter the element: ");
    scanf("%d",&n);

      int[size];

    for (int i = 1; i <=size ; i++)
    {
    
        sum+=n+size[i];
        printf("%d\n",sum);
    }
    
}
